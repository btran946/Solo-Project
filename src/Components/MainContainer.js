import React from 'react';
import Form from './Form';
import Header from './Header';
const MainContainer = () => {
  return (
    <div>
      <Header />
      <Form />
    </div>
  );
};

export default MainContainer;
